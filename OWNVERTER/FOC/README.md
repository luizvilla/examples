# OwnVerter FOC Examples

This folder contains the staged Field Oriented Control examples for OwnVerter.

The sequence starts with open-loop `Vq` excitation, then adds `Iq` current regulation, then closes the outer speed loop.
