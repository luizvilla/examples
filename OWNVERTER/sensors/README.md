# OwnVerter Sensor Examples

This folder introduces rotor position sensing on OwnVerter.

The first example focuses on Hall sensors only and keeps the power stage disabled so the user can observe angle and speed feedback before closing any control loop.
The second example follows the same approach with an incremental ABZ encoder and exposes the raw counter in addition to the Position API angle and speed outputs.
